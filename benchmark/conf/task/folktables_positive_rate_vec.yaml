task: equalized_odds_vec
seed: 0
batch_size: 36
algorithms: [adam, pbm, alm_proj, alm_max, ssg]

constraint:
  name: FairretAgg
  bound: 0.2
  loss: NormLoss
  statistic: PositiveRate
  kwargs:
    uses_labels: false

hydra:
  run:
    dir: .
  job:
    chdir: false