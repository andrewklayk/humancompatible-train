task: weight_norm
seed: 0
batch_size: 32
algorithms: [adam, pbm, alm_proj, alm_max, ssg]

# constraint of weight is hardcoded in the run_benchmark script

hydra:
  run:
    dir: .
  job:
    chdir: false
