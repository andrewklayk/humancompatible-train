# name of the algorithm class to import form src.algorithms
import_name: fairret

params:
  statistic: Accuracy
  loss: NormLoss
  lr: 0.05
  epochs: 10
  pmult: 0.5
  obj_batch_size: 16
  c_batch_size: 8
  save_state_interval: 1000