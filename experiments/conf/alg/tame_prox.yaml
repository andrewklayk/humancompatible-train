# name of the algorithm class to import form src.algorithms
import_name: TameALM
save_name: TameProx

params:
  batch_size: 8
  epochs: null
  lambda_bound: 10
  mu: 2.
  rho: 1.
  tau: 0.05
  eta: 10.0
  beta: 10.0
  mapping: "prox"